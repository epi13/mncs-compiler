mncs 0.8;

// mncs.compiler.main.v1
//
// Minimal repository entry scaffold. Real compiler behavior will be added in
// measured stages and compared against the Rust Stage-0 implementation.
module mncs.compiler.main.v1;

fn main() -> (result: u64) {
    return 0;
}
